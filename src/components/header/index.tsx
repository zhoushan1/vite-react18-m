import { NavBar } from 'antd-mobile'
import { UnorderedListOutline } from 'antd-mobile-icons'

const Header = () => {
  return <NavBar backArrow={<UnorderedListOutline />}>标题</NavBar>
}

export default Header
